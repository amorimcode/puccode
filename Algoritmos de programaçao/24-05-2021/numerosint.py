def quantosElementosSimples(lista):
    qts = 0

    for elemento in lista:
        if type(elemento) is not lista
        qts += 1
        else:
            qnts += quantosElementosSimples(elemento)
